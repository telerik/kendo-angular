export interface PieData {
  category: string;
  value: number;
}

export interface StockChartRecord {
  volume: number;
  open: number;
  close: number;
  high: number;
  low: number;
  date: string;
}
